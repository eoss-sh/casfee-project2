export default interface singelCourse {
    course : {
        name: string,
        shortDesc: string,
        uid: string, 
        desc: string, 
        type: string, 
        url: string,
        error: string,
    }
}